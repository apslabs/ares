<%= semantic_form_for(@condicioniva) do |f| %>
<%= f.semantic_errors %>

<%= f.inputs do %>
  <%= f.input :detalle %>
  <%= f.input :letra, :label => true, :as => :radio, :collection => ["A", "B", "C", "E"] %>
<% end %> 

<%= f.buttons %>
<% end %>
