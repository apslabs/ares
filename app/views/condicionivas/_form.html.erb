<%= form_for(@condicioniva) do |f| %>
  <% if @condicioniva.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@condicioniva.errors.count, "error") %> prohibited this condicioniva from being saved:</h2>

      <ul>
      <% @condicioniva.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :detalle %><br />
    <%= f.text_field :detalle %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
